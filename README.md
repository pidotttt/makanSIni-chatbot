# status
This project is actively being improved
# project structure
- `app.py` – Main Streamlit application
- `models/` – Recommendation and scoring logic
- `data/` – Restaurant survey dataset
- `legacy/` – Older experimental files
# how to run 
pip install -r requirements.txt
streamlit run app.py
